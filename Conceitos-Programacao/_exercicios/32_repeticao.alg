inicio

    inteiro soma, numero

    soma <- 0


    para numero de 1 até 500 passo 1

        se (numero % 2 = 0) entao
            soma <- soma + numero
        senao

    escrever "A soma dos números pares de 1 a 500 é: ", soma

fim